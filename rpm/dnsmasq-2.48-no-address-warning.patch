--- src/dhcp.c.orig	2011-08-17 09:58:15.000000000 +0800
+++ src/dhcp.c	2011-08-17 09:58:51.000000000 +0800
@@ -223,7 +223,9 @@ void dhcp_packet(time_t now)
     {
       if (ioctl(daemon->dhcpfd, SIOCGIFADDR, &ifr) == -1)
 	{
+#if 0
 	  my_syslog(MS_DHCP | LOG_WARNING, _("DHCP packet received on %s which has no address"), ifr.ifr_name);
+#endif
 	  return;
 	}
       else
